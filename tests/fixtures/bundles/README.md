# Canonical bundle cases

이 디렉터리는 실제 지원자 자료가 아닌 합성 case 정의만 보관한다. 테스트는 `cases.json`의
입력으로 임시 Evidence Bundle을 생성하므로 UUID와 시각을 저장소에 고정하지 않으면서도
PASS, FAIL, missing, conflict, aborted, restore-failed 의미를 동일하게 재현한다.
